controller_manager:
  ros__parameters:
    update_rate: 500  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    joint_trajectory_controller:
      type: joint_trajectory_controller/JointTrajectoryController


joint_trajectory_controller:
  ros__parameters:
    joints:
      - shoulder_pan_joint
      - shoulder_lift_joint
      - elbow_joint
      - wrist_1_joint
      - wrist_2_joint
      - wrist_3_joint
    command_interfaces:
      - position
    state_interfaces:
      - position
      - velocity
    state_publish_rate: 100.0
    action_monitor_rate: 20.0
    allow_partial_joints_goal: false
    # Tolerances DISABLED (0.0) so single-point goals don't abort in sim.
    constraints:
      stopped_velocity_tolerance: 0.0
      goal_time: 0.0
      shoulder_pan_joint:  { trajectory: 0.0, goal: 0.0 }
      shoulder_lift_joint: { trajectory: 0.0, goal: 0.0 }
      elbow_joint:         { trajectory: 0.0, goal: 0.0 }
      wrist_1_joint:       { trajectory: 0.0, goal: 0.0 }
      wrist_2_joint:       { trajectory: 0.0, goal: 0.0 }
      wrist_3_joint:       { trajectory: 0.0, goal: 0.0 }
